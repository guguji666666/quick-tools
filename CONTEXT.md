# 当前进度

正在从零创建 Quick Tools，一个部署到 GitHub Pages 的浏览器小工具集合。

## 最近完成

- Git 仓库已初始化，远端 `origin` 已设置为 `git@github.com:guguji666666/quick-tools.git`。
- 建立无构建静态项目。
- 完成左侧工具列表、右侧工具详情布局。
- 完成 YAML Diff 首版：Worker 解析 YAML，语义比对，虚拟列表显示结果。
- 核心比对测试已通过，本地服务已启动在 `http://127.0.0.1:5173`。
- 修复 `file://` 打开时“填入示例”无反应的问题，原因是模块脚本在本地文件打开时不稳定。
- 修复结果区空状态文字叠到差异行上的问题。
- 已用 Computer 验证：`file://` 下示例可填，HTTP 下示例和比对都正常。
- YAML Diff 增加多会话：可新建、切换、关闭会话，每个会话独立保存输入、统计和结果。
- 已用 Computer 验证：HTTP 页面可显示会话条，新建会话、填入示例、比对、切换会话正常。
- YAML Diff 改为左右输入区原地显示比对结果，按路径直接显示旧内容和新内容，并用颜色标注新增、删除、修改。
- 比对继续基于 YAML 解析后的对象，不做纯文本比对，所以空行、缩进、注释、键顺序等格式差异不会被标出来。
- 已用 Computer 验证：示例比对后左右输入区会直接切成对照结果，不再显示底部结果区。
- 优化会话条：新建会话和关闭按钮拉开距离，关闭会话后可点击“撤销关闭”恢复。
- 已用 Computer 验证：新建、关闭、撤销关闭正常。
- 增加中英文语言切换，覆盖页面静态文案、按钮、提示和会话状态，并记住用户选择。
- 已用 Computer 验证：`file://` 页面中英文切换正常。
- 将 IT Tools 从外链入口改成内嵌工具工作台。
- IT Tools 当前内嵌 JSON 格式化/压缩、Base64 编码/解码、URL 编码/解码、SHA-256、UUID、毫秒时间戳转换、文本大小写转换。
- IT Tools 不整包搬入当前项目，避免引入重依赖和 GPLv3 改造发布约束；当前按需实现常用工具。
- 总览工具列表和 IT Tools 内嵌工具列表已改为拖拽排序，顺序保存到浏览器本地状态。
- 拖拽排序已优化为卡片跟随鼠标移动，占位块留在列表内，其他工具补位时有动画。
- 当前环境没有 `npm`，已改用 Codex 自带 Node 直接运行核心测试，结果 3 个通过。
- 本次已通过 `src/app.js` 语法检查、YAML 核心测试 3 个通过；Computer 已确认箭头按钮移除，桌面自动化拖拽对网页事件不稳定。

## 关键决定

- YAML Diff 采用语义比对，不采用纯文本比对，避免缩进、键顺序、引号变化造成噪音。
- 解析和比对放到 Web Worker，保证大文件时界面不被卡住。
- 当前不做构建流程，方便直接部署 GitHub Pages。
- 真正比对必须用 HTTP/GitHub Pages 打开，不能依赖 `file://`。
- 多会话只存在当前页面内存中，刷新页面会清空。
- 最近一次关闭的会话只保存在内存里，用于撤销关闭。
- 语言选择保存在浏览器 `localStorage`，不保存用户粘贴的 YAML 内容。
- 工具排序只保存工具 id，不保存工具输入内容。
- IT Tools 按需实现常用小工具，不做外链代替。
- 时间戳转换只接受毫秒，避免自动猜测秒/毫秒。

## 下次继续

- 根据实际使用反馈补充更多小工具或调整现有工具交互。
